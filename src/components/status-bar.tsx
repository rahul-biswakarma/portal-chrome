import { Button } from './ui/button';

export const StatusBar = () => {
  return (
    <div className="flex items-center rounded-md p-2 bg-card">
      StatusBar
      <Button>Hello</Button>
    </div>
  );
};
