import { Button } from './ui/button';

export const StatusBar = () => {
  return (
    <div className="flex items-center rounded-md p-2 bg-card max-h-[100px] h-full">
      StatusBar
      <Button>Hello</Button>
    </div>
  );
};
